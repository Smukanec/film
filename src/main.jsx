import React from "react";
import ReactDOM from "react-dom/client";
import KostymApp from "./KostymApp";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <KostymApp />
  </React.StrictMode>
);
